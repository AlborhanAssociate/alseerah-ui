# تقرير إعادة تصميم واجهات «السيرة ai»

هذا التقرير يُرافق كود الواجهات في مشروع `alseerah-chat-ui`، ويشرح ما تغيّر عن الموقع الحيّ https://alseerah.ai ولماذا، وكيف يُنقل إلى المشروع الأصليّ.

> الحالة: نسخة موازية جاهزة للنقل. ثلاث شاشات مكتملة: الهبوط، والدخول، والمحادثة. النمطان الفاتح والداكن في الشاشات كلّها.

## ١. المرجع والهدف

- **المرجع**: الموقع الحيّ مبنيّ بـ Vite + React + Tailwind v3 + lucide-react + i18next بأربع لغات وستّة مظاهر، وخلفيّة `/api/v1`.
- **الهدف**: تحديث الواجهة إلى مستوى مساعدات المحادثة الرائدة في بنيتها، مع الحفاظ على وقار منصّةٍ علميّة شرعيّة.
- **الطريقة**: بناء نسخة موازية بالتقنيّات نفسها حتّى تُنقل المكوّنات نسخاً لا ترجمة. كلّ مكوّن «صامت»: يستقبل بياناته ويُصدر أحداثه، وشاشة المحادثة وحدها تعرف طبقة البيانات.

## ٢. التقنيّات

| الطبقة | النسخة |
|---|---|
| Vite | 8 |
| React | 19 |
| TypeScript | 6 |
| Tailwind CSS | 3.4 مع `darkMode: ["class", '[data-theme="dark"]']` |
| الأيقونات | lucide-react، سماكة 1.75 وأحجام 14/16/18 |
| التنقّل | react-router-dom 7 |
| أدوات | clsx + tailwind-merge |

## ٣. نظام التصميم

### الهويّة
- **اللوحة من الشعار**: حبر `#133543`، ذهب `#99762c`، سيبيا `#72462b`، رمليّ `#ece2c8`. بديلاً عن الأخضر «النبويّ» في الموقع الحيّ.
- **خطّ واحد**: IBM Plex Sans Arabic لكلّ الأدوار. لا `letter-spacing` على العربيّة لأنّه يفكّ اتّصال الحروف.
- **لا أوزان ثقيلة** في صفحة الهبوط؛ التباين بالحجم واللون.

### الرموز (tailwind.config.js)
- ألوان `ink` و`paper` و`sand` و`line` والذهب النصّيّ والسيبيا معرَّفة كمتغيّرات CSS بصيغة `rgb(var(--x) / <alpha-value>)`، لها قيمتان في `src/index.css`: على `:root` للفاتح، وعلى `[data-theme="dark"]` للداكن. تبديل النمط يقلب الشاشات كلّها دون تعديل المكوّنات.
- الذهب الفاتح `gold-light` والقيم الثابتة تبقى ثابتةً في النمطَين.
- متغيّر Tailwind إضافيّ `light:` يُفعَّل بـ `[data-theme="light"]` وتستعمله صفحة الهبوط الداكنة بطبيعتها.
- مقياس الزوايا 8/12/16، والحدود شعيرة `ring-1 ring-ink/[0.07]` بدل حدٍّ وظلٍّ معاً، والظلّ عند المرور أو الفتح فقط.

### النمط الفاتح والداكن
- `src/theme.ts`: قراءة/تطبيق/حفظ النمط على `<html data-theme>`، يُطبَّق في `main.tsx` قبل أوّل رسم، ويتبع النظام إن لم يُختَر شيء. المفتاح `alseerah.theme`.
- `src/components/ui/ThemeButton.tsx`: زرّ شمس/هلال مستعمل في الشريط الجانبيّ (المفتوح والمطويّ) وشاشة الدخول وصفحة الهبوط.
- في الداكن: النصوص من درجات الأبيض المحايد، والذهب والسيبيا يُضيئان درجةً حتّى يُقرآ.

## ٤. الشاشات

### ٤.١ الهبوط (`/`) — `src/pages/LandingOnePage.tsx` و`src/components/one/*`
شاشة واحدة بلا تمرير على سطح المكتب، على نسق البينتو:
- الشعار (نصّه أبيض على الداكن) ووصفٌ من سطرَين، ولا هيدر.
- خمس بطاقات في صفَّين على شبكة ١٢ عموداً: بطاقة الهويّة العموديّة (صورة، عبارة «اطرح سؤالك. ودَع المصادرَ تُجيب.»، فقاعات أسئلةٍ زجاجيّة تطفو وتتجاوز حافّة البطاقة، زرّ حبّة «ابدأ محادثتك الآن» بقرصٍ رمليّ يحمل السهم وحوله تموّجات)، وبطاقة «رفيقٌ علميٌّ بين يديك»، وبطاقة «ما لا نفعله»، وبطاقة «٢٣ عاماً»، وبطاقة «أكثر من ٢٠ كتاباً».
- البطاقات النصّيّة زجاجيّة (`backdrop-blur`) بنمطٍ كنتوريّ تجريديّ خفيف (`Doodles.tsx`)؛ المصوّرة بطبقةٍ داكنة تتدرّج من الأسفل إلى شفّافٍ في الأعلى، وحبيبات فيلم وانزياحٍ بطيء للصورة (`.grain`، `.drift`).
- النقر على بطاقةٍ يفتح نافذةً تنطلق من موضعها (`Expanded.tsx`، FLIP بلا مكتبات)؛ الرابط يحمل القسم `#features` وغيره، وEscape والخلفيّة يُغلقان، والتركيز يعود.
- محتوى النوافذ نصٌّ منسَّق بصيغةٍ واحدة (`windows.tsx`): تمهيد ثمّ مداخل برقمٍ عربيّ وعنوان وفقرة. النصوص في `content.ts`.
- خلفيّة الصفحة صورةٌ مموّهة تحت طبقةٍ داكنة، والزوايا 9 بكسل.
- التذييل يحمل شعار جمعيّة البرهان (الأبيض على الداكن والملوّن على الفاتح) مع عبارة «إحدى مبادرات جمعية البرهان لخدمة السُنّة والقرآن» ورابطاً إلى موقع الجمعيّة.
- الهبوط السابق ذو الأقسام محفوظٌ على `/classic` (`LandingPage.tsx` و`components/home/*`) للمقارنة، و`/bento` يُحوّل إلى `/`.

### ٤.٢ الدخول (`/login`) — `src/pages/LoginPage.tsx`
- لوحان داخل هامشٍ من حواف الشاشة: النموذج على اليمين، ولوحة صورةٍ مستديرة على اليسار بألوانٍ ثابتة في النمطَين وثلاث رقاعٍ في أسفلها.
- حقول أعلى، ورابط الشروط والخصوصيّة في الأسفل، وزرّ النمط بجوار «العودة للرئيسيّة».
- **نموذج مؤقّت**: `src/auth/session.ts` يحفظ `{name,email}` في `localStorage` تحت `alseerah.session`. عند النقل تُستبدل `signIn/signOut` بنداءات الخادم، ويأتي اسم العرض من ملفّ المستخدم.

### ٤.٣ المحادثة (`/chat`) — `src/components/chat/*`
- **لا هيدر على لوحة المحتوى** على سطح المكتب؛ كلّ معلومات المحادثة وأزرارها في الشريط الجانبيّ.
- **الشريط الجانبيّ** (`ChatSidebar.tsx`): الشعار (رابط إلى الهبوط) → «محادثة جديدة» و«البحث في المحادثات» صفّان بأيقونةٍ وعنوان → قائمة الجلسات بعناوين مجموعاتٍ لاصقة، والصفّ النشط برمليّ وأيقونة `MessageSquareText` ذهبيّة → بطاقة الحساب بقائمةٍ تنفتح لأعلى (الرئيسيّة، الملفّ الشخصيّ، تسجيل الخروج). حالة مطويّة إلى شريطٍ أيقونيّ. حُذف صندوق النسخة التجريبيّة و«أمّهات الكتب» وزرّ التصدير.
- **الخيط** متمركز بعرض `46rem` ويتلاشى تدريجيّاً قبل حقل الإنشاء (`.fade-to-composer`). ارتفاع الشاشة ثابت والتمرير داخليّ.
- **حالة الترحيب** (`WelcomeState.tsx`): تحيّة بالاسم الأوّل، وأربع بطاقات اقتراح بلا أيقونات على هيئة «ألواحٍ حجريّة»: حافّة متموّجة بمرشّح SVG مختلف لكلّ بطاقة، وسماكة خلفيّة، ونسيج رقّ، بأربع درجاتٍ فاتحة من الرمليّ الذهبيّ. المرشّح على طبقة الخلفيّة فقط فيبقى النصّ حادّاً.
- **الإجابة** (`AgentPlan`, `AnswerBody`, `CitationCard`, `CitationList`): «خطوات الإجابة» بدل «خطّة الوكيل»، عناوين مرقّمة بشاراتٍ موزونة، اقتباساتٌ بخطٍّ ذهبيّ جانبيّ، والمصادر بطاقاتٌ متراصّة في عمودٍ واحد تُفتح واحدةً في كلّ مرّة، والمصدر المفتوح يعرض المقطع والأعلام والحواشي في عمودَين على الشاشات المتوسّطة.
- **حقل الإنشاء** (`ChatComposer.tsx`): ميكروفون وإرسال فقط، سهم الإرسال باتّجاه القراءة، لا سطر تنبيه ولا رقاع أدوات.
- **من الهبوط إلى المحادثة**: `src/lib/pendingQuestion.ts` يحفظ سؤالاً كُتب قبل الوصول إلى المحادثة في `sessionStorage`، وشاشة المحادثة تستهلكه مرّةً واحدة عند الفتح، فينجو من التحويل إلى الدخول. عند النقل يُستبدل السطران بنداء الإرسال الحقيقيّ.
- الصورة الرمزيّة (`ui/Avatar.tsx`) تعود إلى الحرف الأوّل إن تعذّرت الصورة.

### ٤.٤ صفحات مساندة
- `/privacy` و`/terms` (`LegalPage.tsx`): عنوانان و«النصّ قيد الإعداد» مع بريد التواصل.
- `router/ScrollToHash.tsx`: تمريرٌ سلس إلى المراسي مع react-router.

## ٥. الصور والحقوق
- الشعار والعلامات من ملفّات العميل.
- الصور في `public/*.jpg` و`public/tmp/*` **مؤقّتة للمعاينة**؛ مصادرها ورخصها في `public/tmp/CREDITS.md`. صور Unsplash برخصةٍ حرّة ويُستحسن ذكر المصوّر؛ وما قدّمه العميل مُعلَّم كذلك. تُستبدل بصور المنصّة النهائيّة قبل الإطلاق.
- `public/avatar.jpg` صورة العميل مقصوصةً إلى مربّع 512.

## ٦. النقل إلى المشروع الأصليّ
1. نسخ `tailwind.config.js` (الألوان والزوايا والظلال ومتغيّر `light` وإعداد `darkMode`) وكتلة المتغيّرات في `src/index.css`.
2. نسخ `src/components/{chat,ui,brand,one}`، و`src/pages/{LandingOnePage,LoginPage,LegalPage}`، و`src/theme.ts`، و`src/lib/*`، و`src/router/ScrollToHash.tsx`.
3. ربط البيانات: `ChatScreen.tsx` هو الموضع الوحيد الذي يعرف الرسائل والجلسات (`src/data/conversation.ts` نموذج). الأنواع في `src/types.ts` مطابقة لما يُرجعه `/api/v1/chat`.
4. استبدال `auth/session.ts` بمصادقة الخادم، وتفعيل الاسم من ملفّ المستخدم.
5. إعادة تفعيل i18next إن أُريدت اللغات الأربع؛ النصوص العربيّة في `content.ts` وداخل المكوّنات.
6. استبدال الصور المؤقّتة.

## ٧. ما لم يُنجَز أو يحتاج قراراً
- زرّ اللغة «EN» في الهبوط السابق لا يعمل (بلا i18next).
- لا صفحة إدارة `/admin` في النسخة الموازية.
- المكوّن `home/Ornament.tsx` لم يعد مستعملاً في الهبوط السابق؛ يُحذف عند النقل.
- الحساب التجريبيّ يقبل أيّ بريدٍ صحيح الصيغة وكلمة مرورٍ من ثمانية أحرف.

## ٨. الانعكاس على تطبيقات الجوّال
الواجهات مصمَّمة بحيث تُنقل إلى تطبيقات iOS وAndroid إن أُريد ذلك، على ثلاثة مستويات:

- **نظام التصميم قابلٌ للنقل كما هو**: الألوان بقيمتَيها الفاتحة والداكنة، ومقياس الخطّ والزوايا والمسافات، وقواعد الأيقونات (lucide متوافرة لـ React Native وFlutter وSwiftUI وCompose). تُصدَّر من `tailwind.config.js` و`src/index.css` إلى ملفّ رموزٍ واحد تقرأه أيّ منصّة.
- **البنية جاهزة للجوّال أصلاً**: الشريط الجانبيّ يتحوّل إلى درجٍ جانبيّ وشريطٍ علويّ (`MobileBar`)، ونوافذ الهبوط تُفتح كأوراقٍ سفليّة، والخيط متمركز والمُنشئ ثابت في الأسفل. هذه هي أنماط الجوّال المعتادة، فالتصميم لا يحتاج إعادة تفكير، بل إعادة تنفيذ.
- **المكوّنات «صامتة»**: كلّ مكوّن يستقبل بيانات ويُصدر أحداثاً ولا يعرف الخادم، والأنواع في `src/types.ts` مطابقة للواجهة البرمجيّة. فمنطق الشاشات يُنقل إلى React Native بأقلّ تغيير، أو يُعاد كتابته في Flutter أو SwiftUI بالمواصفة نفسها.

خياران عمليّان بحسب الوقت:
1. **الأسرع**: تغليف تطبيق الويب في تطبيقٍ أصليّ (Capacitor أو WebView) مع تفعيل PWA؛ الواجهة تعمل على الجوّال اليوم لأنّها متجاوبة وتحترم مناطق الأمان.
2. **الأكمل**: تطبيق React Native يشترك مع الويب في الأنواع ومنطق الحالة وملفّ الرموز، ويُعاد بناء طبقة العرض بمكوّناتٍ أصليّة على المواصفة نفسها.
